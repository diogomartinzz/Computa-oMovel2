# API Usage

## External API
Picsum Photos API

## API endpoint
`GET https://picsum.photos/v2/list?page={page}&limit={limit}`

- Returns a JSON array of image objects containing `id`, `author`, `width`, `height`, `url`, and `download_url`.
